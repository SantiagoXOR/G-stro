/**
 * Configuración de Supabase para scripts
 * 
 * Copia este archivo a supabase-config.js y completa con tus credenciales
 */

module.exports = {
  // URL de tu proyecto Supabase (sin slash al final)
  supabaseUrl: 'https://myjqdrrqfdugzmuejypz.supabase.co',
  
  // Clave anónima (para operaciones públicas)
  supabaseAnonKey: 'tu-clave-anon-key-aquí',
  
  // Clave de servicio (para operaciones administrativas)
  // IMPORTANTE: Mantén esta clave segura y nunca la expongas en el frontend
  supabaseServiceKey: 'tu-clave-service-role-key-aquí',
};
